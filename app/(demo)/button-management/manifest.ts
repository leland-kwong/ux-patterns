import type { DemoManifest } from '@/app/(demo)/types'

export const manifest: DemoManifest = {
  href: '/button-management',
  label: 'Button Management',
  thesis: `Button sprawl isn't a design problem—it's a decision-making failure exposed at the UI layer.`,
  tags: ['ui_clutter', 'ui_complexity']
}
