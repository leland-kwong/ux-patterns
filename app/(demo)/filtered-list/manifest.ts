import type { DemoManifest } from '@/app/(demo)/types'

export const manifest: DemoManifest = {
  href: '/filtered-list',
  label: 'Filtered List',
  thesis:
    'If users are scanning, virtual scrolling is already the wrong abstraction.',
  tags: ['find_things']
}
