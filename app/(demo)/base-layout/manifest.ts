import type { DemoManifest } from '@/app/(demo)/types'

export const manifest: DemoManifest = {
  href: '/base-layout',
  label: 'Base Layout',
  thesis:
    'Most enterprise UI problems are downstream of a broken layout foundation—especially undefined scroll boundaries and ad-hoc panel composition.',
  tags: ['scroll_broken', 'layout_foundation']
}
