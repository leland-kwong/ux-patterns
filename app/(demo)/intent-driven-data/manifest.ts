import { DemoManifest } from '@/app/(demo)/types'

export const manifest: DemoManifest = {
  label: 'Intent-driven Data',
  href: '/intent-driven-data',
  thesis:
    'Most dashboards try to show everything in case something important is missed. The result is a surface that answers no specific question and forces users to assemble meaning on their own.',
  tags: ['ui_clutter', 'ui_complexity']
}
